# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** นายภานุพงษ์ ทองดี  
**รหัส:** 68543210016-0  
**Sec:** 1  
**ชุด:** A

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

---

## บันทึกของฉัน

| เวลา | งาน (B1/B2/B3/B4) | ถาม AI ว่าอะไร | AI ตอบว่าอะไร / ใช้คำตอบส่วนไหน | แก้เอง/ตรวจสอบอย่างไร |
|---|---|---|---|---|
| 11:38 | B1 | โยน Error ใน Console ให้เช็ค | AI บอกว่าให้เพิ่ม `key` ในส่วนของการ `.map()` | เช็คใน Console |
| 11:49 | B2 | - (`ไม่ได้ใช้ AI`) | แค่เปลี่ยนจาก `completed` เป็น `pending` | เช็คผ่านหน้าเว็บไซต์ได้เลย |
| 12:01 | B3 | โยนโค้ดของ `FilterBar` ไปให้เช็คว่าทำไมค่าสถานะที่กดทำไมไม่ตรงกัน | AI ชี้ให้ไปแก้ที่ `DashboardPage` บรรทัด `filteredRequests` ตรงเครื่องหมาย `!==` แก้ให้เป็น `===` | เช็คผ่านหน้าเว็บไซต์ได้เลย |
| 12:46 | B4 | โยนโค้ดของ `RequestDetailPage` ไปให้เช็คว่าตรวจสอบให้หน่อยว่าทำไมถึงค้นหา URL REQ-001 ขึ้นแค่อันเดียวแต่ตัวอื่นไม่ขึ้น | AI ให้ไปแก้ที่ `RequestDetailPage` โดยเพิ่ม `requestId` ใน `useEffect` | เช็คผ่านการพิมพ์ URL REQ-001 (สุ่มตัวเลข) |
|13:43|B5|โยนโค้ดของ `Dashboard` ไปให้เช็คว่าทำไมถึงลบแล้ว ข้อมูลไม่หาย แต่รีเฟรซแล้วหายตรวจบัคให้หน่อย|AI ให้แก้`handleDelete`ให้แก้ข้อมูลเข้าไปเซ็ต State โค้ดเดิม: `setRequests(requests);` ให้แก้ข้างในเป็น `nextRequests` | ทดสอบลบคำร้องผ่านหน้าเว็บไซต์ของเรา|
| 14:40|B6|โยนโค้ดของ `Dashboard` ไปให้เช็คว่าทำไมกดปุ่มDemoแล้ว จอขาว แต่รีเฟรซแล้วหายตรวจบัคให้หน่อย|เปลี่ยนชื่อตัวแปรที่รับค่า const resetRequests ให้เป็นชื่ออื่น เช่น const freshRequests เพื่อไม่ให้ชื่อตัวแปรไปซ้ำกับชื่อฟังก์ชัน resetRequests() ครับ|ทดสอบกดปุ่มDemoผ่านหน้าเว็บไซต์ของเรา|
|14:57|CP-B2.1| โยนโจทย์เข้าไป + โค้ด `Dashboard`| เพิ่ม State ต่อจาก statusFilter:`(const [searchQuery, setSearchQuery] = useState('');`,ใส่ `<input>` ไว้เหนือ `<RequestList>` `(ใต้ div.section-heading):`|เช็คผ่านหน้าเว็บไซต์ได้เลย|
|15:11|CP-B2.2|B2.2 เพิ่มตรงไหน + โค้ด `Dashboard`|เปลี่ยนตรงบรรทัด `const filteredRequests = ... (ประมาณบรรทัดที่ 44-46)`ก็อปโค้ดวาง|ลองพิมพ์คำว่า ห้อง ในช่องค้นหา รายการต้องถูกกรองเหลือแค่ REQ-002 และ REQ-003
| 15:19|CP-B2.3| ทดสอบตามโจทย์|ทดสอบตามโจทย์|เลือกตัวกรองสถานะ: คลิกเลือกสถานะเป็น `completed` พิมพ์ค้นหา: พิมพ์คำว่า ห้อง ในช่องค้นหา ตรวจสอบผลลัพธ์: รายการคำร้องต้องกรองเหลือเฉพาะ `REQ-003` ใบเดียว (ตรงตามเงื่อนไขทั้งสองอย่าง)
| 15:26|CP-B2.4|โยนโค้ด Dashboard ไปถามว่าต้องใส่เงื่อนไขกรณีค้นหาไม่เจอตรงไหน|AI ให้เพิ่ม Ternary Operator เช็ค filteredRequests.length === 0 เพื่อแสดงข้อความ "ไม่พบคำร้องที่ตรงกับการค้นหา"|พิมพ์ "zzz" ในช่องค้นหา หน้าจอขึ้นข้อความไม่พบคำร้อง แต่แผง SummaryPanel ด้านบนยังแสดงตัวเลข 5/3/1/1 เท่าเดิม|

เมื่อพิมพ์แล้วได้ผลถูกต้อง
---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** นายภานุพงษ์ ทองดี