# AI Usage Log — สอบกลางภาค

ชื่อ-รหัส: นายภานุพงษ์ ทองดี  68543210016-0

บันทึกทุกครั้งที่ใช้ AI ระหว่างสอบ

| เวลา | งาน (B1/B2/B3/B4) | ถาม AI ว่าอะไร | ใช้คำตอบส่วนไหน | แก้เอง/ตรวจสอบอย่างไร |
|---|---|---|---|---|
| 11:38|B1|โยนError ใน Console ให้เช็ค|AI บอกว่าให้เพื่มKeyในส่วนของการ`.map()`|เช็คใน Console|
| 11:49|B2| -|แค่เปลี่ยนจาก `completed` เป็น `pending`|เช็คผ่านหน้าเว็บไซต์ได้เลย|
| 12:01|B3|โยนโค้ดของFilterBarไปให้เช็คว่าทำไมค่าสถานะที่กดทำไมไม่ตรงกัน|โดยAI ชี้ใช้ไปแก้ที่ `DashboardPage` บรรทัด `filteredRequests` ตรงเครื่องหมาย !== แก้ให้เป็น ===|เช็คผ่านหน้าเว็บไซต์ได้เลย|
| 12:46|B4|โยนโค้ดของ`RequestDetailPage`ไปให้เช็คว่าตรวจสอบให้หน่อยว่าทำไมถึงค้นหา URL REQ-001 ขึ้นแค่อันเดียวแต่ตัวอื่นไม่ขึ้น|โดยAI ใช้ไปแก้รอบแรก `RequestDetailPage` โดยเพิ่ม `requestId` ในuseEffect |เช็คผ่านการพิมพ์ url REQ-001(สุ่มตัวเลข)|
| | | | | |