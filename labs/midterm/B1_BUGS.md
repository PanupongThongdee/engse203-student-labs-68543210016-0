# B1 · บันทึกการแก้บั๊ก (กรอกให้ครบทั้ง 6 จุด)

> แต่ละบั๊กให้เขียน 4 อย่าง: ไฟล์ · บรรทัด · สาเหตุ (ทำไมทำงานผิด) · แก้อย่างไร
> เขียนด้วยคำของตัวเอง — จุดนี้จะถูกถามใน oral

## บั๊กที่ 1 — อาการ: Console เตือนสีเหลืองเรื่องรายการ
- ไฟล์/บรรทัด: `components/RequestList` (บรรทัดที่ 8)
- สาเหตุ: สาเหตุที่ขึ้น Warning: เมื่อคุณไม่ได้ใส่ key={...} ให้กับ Element ตัวนอกสุดใน .map() Reactจะไม่สามารถระบุตัวตนของ Element แต่ละตัวได้อย่างแม่นยำ
- แก้อย่างไร: โดยแก้ไขโดยเพิ่ม Key ในบรรทัดที่ 8 เนื่องจากถ้าไม่มี key  ตัว  react จะไม่สามารถระบุตัวตนของ Element แต่ละตัวได้อย่างแม่นยำ `<div key={request.id} className="request-item">`

## บั๊กที่ 2 — อาการ: ตัวเลข "รอดำเนินการ" ในแผงสรุปไม่ตรงกับที่เห็น
- ไฟล์/บรรทัด: `pages/DashboardPage` (บรรทัดที่ 47)
- สาเหตุ: ในบรรทัดที่ 47 มีการใส่ การ.filter เช็ค status === "completed" ซ้ำกับบรรทัดที่  49 เลยทำให้แสดงจำนวนสถานะไม่ตรงกับข้อมูล
- แก้อย่างไร: เปลี่ยนจาก completed เป็น ending `pending: requests.filter((request) => request.status === 'pending').length`

## บั๊กที่ 3 — อาการ: กดตัวกรอง "รอดำเนินการ" แล้วได้รายการที่ไม่ใช่
- ไฟล์/บรรทัด: `pages/Dashboardpage` (บรรทัดที่ 54)
- สาเหตุ: ในบรรทัดที่ 54 มี bug เครื่องหมาย !==   จึงทำให้ ตัวดัคกรองคำร้องไม่ตรงกัน
- แก้อย่างไร: เปลี่ยนเครื่องหมาย จาก  !== เป็น === แทน

## บั๊กที่ 4 — อาการ: เปลี่ยน URL จาก REQ-001 เป็น REQ-002 แล้วข้อมูลไม่เปลี่ยน
- ไฟล์/บรรทัด:
- สาเหตุ:
- แก้อย่างไร:

## บั๊กที่ 5 — อาการ: กด "ลบ" แล้วรายการยังอยู่ ต้องรีเฟรชถึงหาย
- ไฟล์/บรรทัด:
- สาเหตุ:
- แก้อย่างไร:

## บั๊กที่ 6 — อาการ: กด "Reset Demo Data" แล้วหน้าพัง/ว่างเปล่า
- ไฟล์/บรรทัด:
- สาเหตุ:
- แก้อย่างไร:
